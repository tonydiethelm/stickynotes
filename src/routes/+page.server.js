/** @type {import('./$types').PageServerLoad} */
export async function load() {
    return {};
};


/*
I need to take in user state from the page and save it as the sticky set in Redis. 
*/


/*
I need to take in a username and a set name, and return the set from Redis to the page.
*/